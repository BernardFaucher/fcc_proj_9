# The data can then be loaded with the rails db:seed command (or created alongside the database with db:setup).

# 15.times do |t|
#     @topic = Topic.create(text: "Topic #{t}", board: "test")
#     (t % 5).times do |r|
#         @topic.replies.create(text: "Reply #{r} to #{t}")
#     end
# end
